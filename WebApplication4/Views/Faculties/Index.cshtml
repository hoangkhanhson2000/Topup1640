@model IEnumerable<WebApplication4.Models.Faculty>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Faculty_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Faculty_Detail)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Faculty_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Faculty_Detail)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FacultyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FacultyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FacultyID })
        </td>
    </tr>
}

</table>
